const form=document.querySelector('#access-form'),feedback=document.querySelector('#feedback'),botao=document.querySelector('#entrar');
form.addEventListener('submit',async(e)=>{e.preventDefault();feedback.textContent='';if(!form.reportValidity())return;botao.disabled=true;botao.textContent='Validando…';try{const response=await fetch('/api/clientes/acesso',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({nome:document.querySelector('#nome').value.trim(),telefone:document.querySelector('#telefone').value.trim(),cpf:document.querySelector('#cpf').value.trim()})});const body=await response.json();if(!response.ok){const mensagem=Array.isArray(body.detail)?body.detail[0]?.msg:body.detail;throw new Error((mensagem||'Confira os dados informados.').replace('Value error, ',''));}location.href='/agendar';}catch(error){feedback.textContent=error.message;feedback.className='feedback error';}finally{botao.disabled=false;botao.textContent='Continuar';}});
